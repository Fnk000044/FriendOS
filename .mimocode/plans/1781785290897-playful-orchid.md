# FriendOS UI 全局统一 + 动画升级计划

## 参考风格

**THRM 冰川科技风**：HUD 网格底纹、扫描线动画、角括号装饰、`backdrop-filter: blur(24px) saturate(118%)`
**WeFlow 精致玻璃风**：87 个 keyframes、窗口入场动画、logo 呼吸、进度条光谱、`cubic-bezier(0.2, 0.8, 0.2, 1)`

**FriendOS 定位**：心理健康管家 → 温暖玻璃风 + 治愈微动效（保留现有 teal 主色调）

---

## 第一步：修复窗口按钮（Critical）

**文件**：`electron/main.cjs:210`

```js
// 当前（错误）
preload: path.join(path.dirname(app.getPath('exe')), 'resources', 'app.asar', 'preload.cjs'),
// 修复
preload: path.join(__dirname, 'preload.cjs'),
```

同时删除重复的 `enableRemoteModule: false`（line 215-216）

---

## 第二步：统一设计系统（HIGH）

### 2.1 统一边圆角

| 用途 | 值 | Tailwind | 替换 |
|------|------|----------|------|
| 卡片/容器 | 16px | `rounded-2xl` | 统一所有 card |
| 按钮/输入框 | 10px | `rounded-[10px]` | 统一所有 button/input |
| 小元素/标签 | 8px | `rounded-lg` | 统一 badge/tab |
| 胶囊 | 9999px | `rounded-full` | avatar/dot |

**需要统一的文件**：
- `EmotionPage.tsx`：tab 按钮 `rounded-md` → `rounded-lg`
- `MemoriesPage.tsx`：tab 按钮 `rounded-md` → `rounded-lg`
- `RiskDashboardPage.tsx`：混合使用 → 统一

### 2.2 统一阴影

| 用途 | 类 | 替换 |
|------|------|------|
| 卡片默认 | `shadow-card` (CSS var) | 替换所有 `shadow-sm`/`shadow-md` |
| 卡片悬停 | `shadow-card-hover` (CSS var) | 统一 hover 态 |
| 浮层/弹窗 | `shadow-float` (CSS var) | 替换 `shadow-xl`/`shadow-2xl` |

### 2.3 统一过渡时长

| 用途 | 值 | 替换 |
|------|------|------|
| 微交互 | 150ms | hover/active 态 |
| 标准交互 | 200ms | 大部分组件 |
| 页面转场 | 300ms | 路由切换 |
| 弹窗 | 400ms | modal/drawer |

---

## 第三步：修复暗色模式（CRITICAL）

**完全损坏的页面**（使用硬编码 `bg-white`/`text-slate-800`）：
- `RiskDashboardPage.tsx`：8 处 `bg-white`、6 处 `text-slate-800`、8 处 `border-slate-100`
- `EmotionPage.tsx`：8 处 `text-slate-800`、6 处 `text-slate-500`
- `TherapyPage.tsx`：7 处 `text-slate-800`、6 处 `bg-slate-50`
- `ReportsPage.tsx`：4 处 `text-slate-800`、3 处 `text-slate-600`

**替换规则**：
| 硬编码 | 替换为 CSS 变量 |
|--------|----------------|
| `bg-white` | `bg-surface-card` 或 `var(--bg-card)` |
| `text-slate-800` | `text-text-primary` |
| `text-slate-700` | `text-text-primary` |
| `text-slate-600` | `text-text-secondary` |
| `text-slate-500` | `text-text-muted` |
| `text-slate-400` | `text-text-muted` |
| `border-slate-100` | `border-[var(--glass-border)]` |
| `bg-slate-50` | `bg-surface-hover` |

---

## 第四步：替换 Emoji 为 SVG 图标（MEDIUM）

**需要替换的文件**：
| 文件 | Emoji | 替换为 |
|------|-------|--------|
| `HabitCard.tsx` | 🔥 | `Flame` from lucide |
| `HabitStreak.tsx` | 🔥 | `Flame` |
| `MoodSelector.tsx` | 😡😞😐😊😄 | 5 个 SVG face 图标 |
| `EmotionPicker.tsx` | 😊😢😠😰😲😌🥰😫 | 8 个 SVG face 图标 |
| `RecentDiary.tsx` | 😡😞😐😊😄 | 同 MoodSelector |
| `SentimentBadge.tsx` | ⚠!😊😐😔😢💡💙✕ | lucide 图标 |
| `ThoughtRecord.tsx` | 😰😢😠😨😳😣😔😞😤🤔💡 | lucide 图标 |
| `BreathingExercise.tsx` | 🎉💡 | `PartyPopper`/`Lightbulb` |
| `MindfulnessSession.tsx` | 💡⚠️ | `Lightbulb`/`AlertTriangle` |
| `HealthRadar.tsx` | 😊😤⚡👥😴💆 | lucide 图标 |
| `EmotionTrend.tsx` | 😊😢😠😰 | SVG face |
| `AISettingsModal.tsx` | 🧠☁️💻💡 | `Brain`/`Cloud`/`Monitor`/`Lightbulb` |
| `AssessmentResult.tsx` | ⚠️ | `AlertTriangle` |
| `AssessmentPage.tsx` | 💡 | `Lightbulb` |
| `ModeSelectionModal.tsx` | 💡 | `Lightbulb` |
| `OnboardingTour.tsx` | 💡 | `Lightbulb` |

---

## 第五步：动画系统升级（MEDIUM）

### 5.1 新增 CSS 动画（参考 THRM/WeFlow）

在 `index.css` 添加：

```css
/* 窗口入场 */
@keyframes windowAppear {
  from { opacity: 0; transform: scale(0.97) translateY(12px); }
  to { opacity: 1; transform: scale(1) translateY(0); }
}

/* 卡片浮入 */
@keyframes cardFloat {
  from { opacity: 0; transform: translateY(16px) scale(0.98); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}

/* 扫描线（THRM 风格） */
@keyframes scanLine {
  0% { transform: translateX(-100%); opacity: 0; }
  50% { opacity: 0.3; }
  100% { transform: translateX(100%); opacity: 0; }
}

/* 呼吸发光 */
@keyframes breatheGlow {
  0%, 100% { box-shadow: 0 0 0 0 var(--color-primary-glow); }
  50% { box-shadow: 0 0 12px 4px var(--color-primary-glow); }
}

/* 进度条光谱 */
@keyframes spectrumSlide {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

/* 弹性出现 */
@keyframes popIn {
  0% { opacity: 0; transform: scale(0.9); }
  70% { transform: scale(1.03); }
  100% { opacity: 1; transform: scale(1); }
}

/* 列表交错入场 */
@keyframes staggerFadeUp {
  from { opacity: 0; transform: translateY(12px); }
  to { opacity: 1; transform: translateY(0); }
}
```

### 5.2 页面转场动画

升级 `AppLayout.tsx` 中的 `page-enter`：
- 使用 `windowAppear` 替代当前的简单 fadeIn
- 添加交错子元素入场

### 5.3 卡片 hover 动画（THRM 风格）

```css
.glass-card-hover:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-card-hover);
  border-color: rgba(20, 184, 166, 0.15);
}
```

### 5.4 标题栏升级（参考 WeFlow）

**TitleBar.tsx**：
- 增强毛玻璃：`backdrop-filter: blur(24px) saturate(1.2)`
- 按钮 hover 过渡：`transition: all 0.15s ease`
- 关闭按钮 hover：红色背景 `#EF4444` + 白色图标
- 添加品牌 logo 在标题栏左侧

---

## 第六步：细节打磨（LOW）

### 6.1 Error 状态统一
- `Input.tsx` 和 `Textarea.tsx` 统一使用 `border-red-500 ring-2 ring-red-500/20`

### 6.2 DailyQuote 改用共享 Modal
- 替换自定义 modal overlay 为 `Modal.tsx` 组件

### 6.3 焦点状态
- 所有交互元素添加 `focus-visible:ring-2 focus-visible:ring-primary`

---

## 执行顺序

1. ✅ 修复 preload 路径 → 窗口按钮恢复
2. 统一设计 token 使用（index.css + tailwind.config.js 不变，改组件）
3. 修复 4 个暗色模式损坏页面
4. 替换 emoji 为 SVG（16 个文件）
5. 添加新动画 keyframes
6. 升级 TitleBar 动画
7. 细节打磨

## 验证

1. `npm run dist` 构建
2. 运行 `release\win-unpacked\FriendOS.exe`
3. 测试窗口按钮（最小化/最大化/关闭）
4. 测试亮色/暗色模式切换
5. 逐页检查动画效果
6. 检查所有 emoji 是否已替换
