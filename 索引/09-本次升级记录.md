<<<<<<< HEAD
# 09 - 本次升级记录（2026-07 全面升级 + 2026-08 庆园杯批次）

> 最后更新日期：2026-08-14  
> 维护者：FriendOS Team

本文件记录升级需求的逐批次执行情况。

---

## Batch 11：庆园杯对照检查落地（已完成 2026-08-14）

### 背景
对照《庆园杯人工智能创新应用大赛指南》与《作品申报书》，结合外部只读审计报告（20 项问题：P0×2/P1×6/P2×12），落地 5 项新功能 + 修复 10 项 bug。

### 完成
1. 《隐私政策》正式页面（/privacy，10 节 + 个保法依据，zh/en 双语）
2. 危机安全计划（/safety-plan，SPI 六步，字段 AES-GCM 加密，schema v13；危机弹窗与高风险卡片联动入口）
3. 心理报告导出 PDF（ReportsPage 按钮 → 渲染层 HTML → 主进程 printToPDF → A4）
4. 量表扩展 ISI-7（失眠严重度）与 CD-RISC-10（心理韧性），通用 ScaleForm 组件
5. 离线心理科普库（/knowledge，14 话题双语知识库 + 加权检索 + 聊天联动直达）
6. 外部审计 P0：备份导出缺 await、导入非原子+回滚、备份补齐 3 张表
7. 外部审计 P1：仪表盘时钟扩散重渲染、每日检查结果丢弃+零值行污染、风险页竞态、ONNX session 泄漏+reset 竞态、chat 请求无上限、日记分析竞态
8. 外部审计 P2：preload 事件 handler 覆盖、storage-size 递归上限、test mock 结构对齐
9. 外部审计 P2-12（追加）：主进程未捕获异常加固——降级状态可见（toast + 自检面板），连续异常优雅退出
10. 外部审计 P2 剩余项清仓：P2-1（保存日记免重复 ONNX，社交分数本地计算）、P2-8（7 处 window.confirm 全部换 ConfirmDialog）、P2-9（健康画像加载骨架屏）、P2-11（记忆库分页渲染）、P2-7（迁移兼容新旧存储布局 + 复制校验 + 启动回退保护）

### 验证
- `npx tsc --noEmit` 通过
- `npm run test`：29 个文件 365 个测试全部通过
=======
# 09 - 本次升级记录（2026-07 全面升级）

> 最后更新日期：2026-07-16  
> 维护者：FriendOS Team

本文件记录本次 10 项需求 + 项目文档升级的逐批次执行情况。**全部已完成。**
>>>>>>> a66c30d430cd26eb226e71f7098d31e9a6a7c193

---

## Batch 0：索引目录 + 文档骨架（已完成 2026-07-15）

### 完成
- 创建 `E:\FriendOS\索引\` 目录及 9 份文档骨架（README + 01~09）。
- 文档基于对现有代码的全面探索，记录架构/DB/AI/引擎/组件/路由/设计系统现状与已知问题。

### 覆盖需求
- 需求 11（项目说明文档）的骨架部分。

---

## Batch 1：Bug 修复 + Token 补全 + useClock + 删 300ms（已完成 2026-07-15）

### 完成
1. 修复 `var(--primary)` → `var(--color-primary)`（Sidebar 2 处 + index.css 2 处）
2. 修复 `ChatMessage.tsx` `stripThinkTags` 正则（` IMD` → ` IMD`）
3. 统一 `Textarea` 错误态为 `border-red-500 ring-2 ring-red-500/20`（与 Input 一致）
4. `TitleBar.tsx` 关闭按钮改 CSS `:hover`，移除命令式 DOM 突变
5. `LockScreen.tsx` 删局部 `@keyframes shake`，复用 `.animate-shake`
6. `AnimatedNumber` 检测 `prefers-reduced-motion` 直接跳终值
7. 新增 `--gradient-primary` token（light+dark），替换 Sidebar/TitleBar/Header 3 处硬编码
8. 新增 `--font-mono` token + JetBrains Mono 栈，`.chat-markdown code` 应用
9. Tailwind 扩展 `fontFamily.mono` + `fontSize` ramp + `letterSpacing`
10. 新建 `useClock.ts` hook（每秒刷新，reduced-motion 降 60s）+ `formatClockDisplay`/`getGreeting`
11. Header/StatusBar 接入 `useClock`，格式 `HH:mm:ss`
12. DashboardPage 问候语接入 `useClock`，跨时段自动更新
13. 删除 `ReportsPage.tsx` 强制 300ms 最小延迟

### 覆盖需求
- 需求 1（UI/UX Bug 修复 + Token 补全）
- 需求 2.2（合并双时钟 → useClock）
- 需求 2.4（删除强制 300ms 延迟）
- 需求 4（顶部时间实时同步，精确到秒）

### 验证
- `npx tsc --noEmit` 通过（仅预存 test/setup.ts vi 报错，与本次无关）
- 详见 `08-变更日志.md` 2026-07-15 Batch 1 条目

---

## Batch 2：仪表盘两列重排 + 孤儿清理 + 双雷达合并（已完成 2026-07-15）

### 完成
1. 删除孤儿组件 4 个：`RecentDiary.tsx` / `HabitSummary.tsx` / `MoodTrend.tsx` / `ProgressRing.tsx`
2. `DashboardPage` 主内容区从 `xl:grid-cols-12` 混合 col-span 重排为严格 `md:grid-cols-2` 两列，DailyQuote 跨两列置底
3. 新建独立 `RiskRadar.tsx` 组件，复用 `HealthRadar` 的 CSS 变量主题色（`--color-radar-stroke/fill`），替代硬编码 `#14B8A6`
4. `RiskDashboardPage` 内联雷达图替换为 `<RiskRadar>`，硬编码中文 → i18n `t('risk.health_dim')`

### 覆盖需求
- 需求 5（仪表盘排版整齐，两列）
- 需求 2.1（清理孤儿组件）
- 需求 2.3（合并两个雷达图）

---

## Batch 3+4：AI 助理优化 + 复盘逻辑流式化（已完成 2026-07-15）

### 完成
1. ChatPanel 回车延迟修复（危机检测异步化，不阻塞 sendMessage）
2. useAI 静默丢弃二次发送改为 abort 上一次再开始新的
3. 首 token 3s 超时计时器（仍无输出才标记重试）
4. AIService 取消强制 `tone='counselor'`，读取 `config.tone`
5. prompts.ts 回复要求 3-5 句 → 3-8 句，必要时可分段或用列表
6. LocalModelProvider 历史窗口 `slice(-4)` → `slice(-8)`（2 轮 → 4 轮）
7. LocalModelProvider `maxTokens` 2048 → 4096
8. WeeklyReportCard 流式重写（打字机 + staggerFadeUp 淡入，删除 spinner）
9. ReportAIService 新增 `generateAIReportStream`，system prompt 加 2 个 few-shot 示例
10. 删除 `WeeklyReportService.ts`（纯规则版，规则仅作兜底保留在 ReportAIService）

### 覆盖需求
- 需求 10（AI 助理输出优化：延迟/内容/速度）
- 需求 9（复盘总结生成逻辑优化：用 Qwen + 升级动效）

---

## Batch 5：ONNX 准确度 + 告警逻辑优化（已完成 2026-07-15）

### 完成
1. `crisisKeywords.cjs` 移除 `想死了`/`想去死` 排除，关键词层命中即 medium
2. 关键词层负例平滑分母 `max(total,8)` → `max(total,4)`
3. ONNX 置信度分层：`>=0.85` 高置信 / `0.55~0.85` 中置信 / `<0.55` 低置信
4. `analyzeEnhanced` 融合策略：高置信 ONNX 为主 / 中置信 ONNX 0.6+关键词 0.4 / 低置信关键词 0.6+ONNX 0.4
5. Qwen few-shot prompt 强化（4 个正反例）
6. DiaryEditor 30s 延迟 → 立即弹窗（真实危机不应延迟）
7. RiskDashboardPage 15s 超时 → 部分渲染 + 非阻塞提示条（非整页 EmptyState）

### 覆盖需求
- 需求 3（提高 ONNX 判断准确度 + 优化告警逻辑）

---

## Batch 6：健康维度图修复 + 心理守护概览清晰化（已完成 2026-07-15）

### 完成
1. 风险评分卡片强化：大色块渐变背景 + 顶部 5 格风险等级条（critical 闪烁）+ 高风险行动指引卡片（热线 400-161-9995）
2. 信号源分析：每信号加 4 态文字标签（良好/正常/需关注/高风险）
3. 风险因素列表：权重 ≥20 严重因素用红底卡片醒目展示
4. 硬编码中文（信号源分析/风险因素/权重）→ i18n
5. 健康维度雷达图修复（Batch 2 抽 RiskRadar 组件 + Batch 5 超时部分渲染）

### 覆盖需求
- 需求 8（健康维度分析的图 + 心理守护概览清晰化，用户能立马感知问题）

---

## Batch 7：任务管理增强（已完成 2026-07-15）

### 完成
1. DB schema v9：tasks 表新增 `sortOrder` 索引；Task 模型新增 `dueTime`/`reminderEnabled`/`subtasks`/`sortOrder`
2. `useTasks` 扩展：`buildTask`/`createTask`/`createRecurringTasks` 支持新字段；新增 `addSubtask`/`toggleSubtask`/`deleteSubtask`/`reorderTasks`
3. `TaskItem` 增强：优先级色条加粗、截止时间倒计时标签、子任务进度条 + 展开列表
4. `TaskDetailModal` 新增：截止时间选择器 + 提醒开关 + 子任务清单编辑（增删改 + 勾选）
5. `TaskBoard` 新建：3 列看板视图（待办/进行中/已完成），HTML5 原生 dragdrop 跨列改状态
6. `TaskCalendar` 新建：月历网格 + 优先级色点 + 当日任务列表
7. `TasksPage` 视图切换 Tab（列表/看板/日历）

### 覆盖需求
- 需求 6（任务管理界面增强：截止时间+提醒、子任务、拖拽/看板、日历视图）

---

## Batch 8：演示数据更新 + 暗色模式激活 + 动效升级（已完成 2026-07-15）

### 完成
1. `seedDemoData.ts` 故事线日记更新到 2026 语境（AI 工具、线上聚餐、AI 助手复习）
2. 移除 `import.meta.env.DEV` 限制，所有模式可加载演示数据（SettingsPage 已有确认弹窗）
3. `AppLayout` 接入 `useThemeStore`，初始化自动 applyTheme + 监听系统变化
4. `Header` 新增主题切换菜单（浅色/深色/跟随系统，Sun/Moon/Monitor）
5. `Card` hover 态加 `card-float` 动画
6. `TaskStats` 修复 `--tw-hover-bg` 未消费 bug

### 覆盖需求
- 需求 7（更新演示数据 + 增加触发入口）
- 需求 1.3（暗色模式激活）
- 需求 1.4（动效升级）

---

## Batch 9：项目说明文档补全（已完成 2026-07-15）

### 完成
1. 全部专题文档（01-07）同步更新至本次升级后的最新状态
2. 已知问题章节标注 ✅ 已修复 / ⚠️ 仍存在
3. 新增组件（RiskRadar/TaskBoard/TaskCalendar）补入组件清单
4. schema v9 补入数据库设计文档
5. AI 子系统/风险引擎文档反映分层置信度/few-shot/告警逻辑改动
6. 设计系统文档补入 `--gradient-primary`/`--font-mono`/fontSize ramp/暗色模式激活状态

### 覆盖需求
- 需求 11（项目说明文档）

---

## 本次升级总结

| 批次 | 需求 | 状态 |
|------|------|------|
| Batch 0 | 11（骨架） | ✅ |
| Batch 1 | 1, 2.2, 2.4, 4 | ✅ |
| Batch 2 | 2.1, 2.3, 5 | ✅ |
| Batch 3+4 | 9, 10 | ✅ |
| Batch 5 | 3 | ✅ |
| Batch 6 | 8 | ✅ |
| Batch 7 | 6 | ✅ |
| Batch 8 | 1.3, 1.4, 7 | ✅ |
| Batch 9 | 11（补全） | ✅ |

**全部 10 项需求 + 项目文档已完成，类型检查通过。**

> Batch 10（在线模型接入超星）已尝试但全部移除：超星 CxRobotSdkJs 是嵌入式聊天浮窗，不提供程序化收发消息接口，prime/embedChat 页面对非授权来源拒绝访问。详见 `08-变更日志.md`。

### 关键改动文件清单（Batch 0-9）
- `pc/src/index.css` — CSS 变量 + keyframes + chat-markdown
- `pc/tailwind.config.js` — fontFamily.mono / fontSize / letterSpacing
- `pc/src/hooks/useClock.ts` — 新建实时时钟 hook
- `pc/src/hooks/useAI.ts` — abort 重发 + 首 token 超时
- `pc/src/services/ai/AIService.ts` — tone 读取用户选择
- `pc/src/services/ai/prompts.ts` — 回复要求放宽
- `pc/src/services/ai/providers/LocalModelProvider.ts` — 历史窗口扩展 + maxTokens
- `pc/src/services/ai/ReportAIService.ts` — 流式生成 + few-shot
- `pc/src/components/reports/WeeklyReportCard.tsx` — 流式重写
- `pc/src/components/emotion/RiskRadar.tsx` — 新建独立雷达组件
- `pc/src/components/tasks/TaskBoard.tsx` — 新建看板视图
- `pc/src/components/tasks/TaskCalendar.tsx` — 新建日历视图
- `pc/src/components/tasks/TaskItem.tsx` — 截止倒计时 + 子任务
- `pc/src/components/tasks/TaskDetailModal.tsx` — 截止/提醒/子任务编辑
- `pc/src/components/tasks/TaskStats.tsx` — hover bug 修复
- `pc/src/pages/DashboardPage.tsx` — 两列重排 + useClock
- `pc/src/pages/RiskDashboardPage.tsx` — 概览强化 + 超时部分渲染 + i18n
- `pc/src/pages/TasksPage.tsx` — 视图切换
- `pc/src/pages/ReportsPage.tsx` — 删 300ms
- `pc/src/components/layout/{Header,AppLayout}.tsx` — useClock + 主题切换
- `pc/src/components/layout/StatusBar.tsx` — useClock
- `pc/src/components/common/{AnimatedNumber,Card,LockScreen,Textarea}.tsx` — Bug 修复
- `pc/src/components/assistant/ChatPanel.tsx` — 危机检测异步化
- `pc/src/components/assistant/ChatMessage.tsx` — stripThinkTags 修复
- `pc/src/components/diary/DiaryEditor.tsx` — 危机弹窗立即触发
- `pc/src/db/{models,index}.ts` — schema v9 + SubTask
- `pc/src/utils/seedDemoData.ts` — 2026 语境 + 去 DEV 限制
- `pc/electron/services/{SentimentService,crisisKeywords}.cjs` — ONNX 分层 + 排除词 + few-shot
- `pc/src/services/ai/WeeklyReportService.ts` — 已删除
