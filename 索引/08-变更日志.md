# 08 - 变更日志

> 最后更新日期：2026-07-15  
> 维护者：FriendOS Team

本文件记录每次代码改动。格式：

```
## YYYY-MM-DD 范围简述
### 改动
- [文件路径] 改动内容（一句话）
### 原因
- 为什么改
### 验证
- 如何验证（编译/运行/手动测试）
```

---

## 2026-07-15 Batch 9：项目说明文档补全

### 改动
- [索引/01-架构总览.md] 无需更新（架构层未变）
- [索引/02-数据库设计.md] 新增 v9 schema 行 + Task 模型新增字段（dueTime/reminderEnabled/subtasks/sortOrder）+ SubTask 接口
- [索引/03-AI子系统.md] AIService tone 改为读取用户选择；ReportAIService 新增 generateAIReportStream；已知限制全部更新（历史窗口/stripThinkTags/tone/WeeklyReportService 删除）
- [索引/04-情绪与风险引擎.md] 置信度分层策略 + 关键词平滑 + 危机排除词移除 + 告警逻辑（DiaryEditor 立即/RiskDashboard 部分渲染）
- [索引/05-组件清单.md] 新增 RiskRadar/TaskBoard/TaskCalendar；孤儿组件标记已删除；WeeklyReportCard 职责更新
- [索引/06-路由与页面.md] DashboardPage 两列 / RiskDashboardPage 强化 / TasksPage 视图切换 / ReportsPage 删 300ms
- [索引/07-设计系统.md] gradient-primary/font-mono/fontSize ramp/暗色模式激活；已知问题全部标记修复状态
- [索引/09-本次升级记录.md] 全部批次记录补全 + 关键改动文件清单

### 原因
- 需求 11（项目说明文档）补全，满足"全局工作要求"的索引同步

### 验证
- 文档为只读 Markdown，无需编译
- 所有代码引用使用 `file_path:line_number` 格式

---

## 2026-07-15 Batch 8：演示数据更新 + 暗色模式激活 + 动效升级

### 改动
- [pc/src/utils/seedDemoData.ts] 故事线日记文本更新到 2026 语境（AI 工具、线上聚餐、AI 助手复习），更有代入感
- [pc/src/utils/seedDemoData.ts:513] 移除 `import.meta.env.DEV` 限制，所有模式可用（SettingsPage 已有确认弹窗防误清空）
- [pc/src/components/layout/AppLayout.tsx:8,15] 接入 `useThemeStore`，初始化时自动 applyTheme + 监听系统主题变化
- [pc/src/components/layout/Header.tsx] 新增主题切换菜单（浅色/深色/跟随系统），Sun/Moon/Monitor 图标
- [pc/src/components/common/Card.tsx:25] hover 态增加 `card-float` 动画类，毛玻璃卡片浮起感更强
- [pc/src/components/tasks/TaskStats.tsx:18-21] 修复 `--tw-hover-bg` 未消费 bug，改为 Tailwind `hover:bg-surface-hover hover:border-[var(--glass-border)]`

### 原因
- 需求 7：更新演示数据到 2026 语境 + 增加触发入口（SettingsPage 已有按钮，去 DEV 限制）
- 需求 1.3：暗色模式 CSS 已就绪但无运行时开关，激活 useThemeStore
- 需求 1.4：动效升级（Card 浮起 + 修复 TaskStats hover bug）

### 验证
- `npx tsc --noEmit` 通过
- Header 主题菜单可切换浅色/深色/跟随系统
- 演示数据可在 SettingsPage 加载（所有模式）

---

## 2026-07-15 Batch 7：任务管理增强（schema v9 + 子任务 + 截止时间 + 看板 + 日历）

### 改动
- [pc/src/db/models.ts:3-8] 新增 `SubTask` 接口
- [pc/src/db/models.ts:10-30] `Task` 接口新增 `dueTime?`/`reminderEnabled?`/`subtasks?`/`sortOrder?` 字段
- [pc/src/db/index.ts:84-89] Dexie v9：tasks 表新增 `sortOrder` 索引（非破坏性，旧数据自动兼容）
- [pc/src/hooks/useTasks.ts] `buildTask`/`createTask`/`createRecurringTasks` 支持 dueTime/reminderEnabled/subtasks/sortOrder；新增 `addSubtask`/`toggleSubtask`/`deleteSubtask`/`reorderTasks`
- [pc/src/components/tasks/TaskItem.tsx] 增强：左侧优先级色条加粗、截止时间倒计时标签（AlarmClock）、子任务进度条 + 展开列表（ListChecks）
- [pc/src/components/tasks/TaskDetailModal.tsx] 新增截止时间选择器 + 提醒开关 + 子任务清单编辑（增删改 + 勾选）
- [pc/src/components/tasks/TaskBoard.tsx] 新建看板视图：3 列（待办/进行中/已完成），HTML5 原生 dragdrop 跨列改状态
- [pc/src/components/tasks/TaskCalendar.tsx] 新建日历视图：月历网格 + 优先级色点 + 当日任务列表
- [pc/src/pages/TasksPage.tsx] 顶部加视图切换 Tab（列表/看板/日历），三种视图按 `viewMode` 切换

### 原因
- 需求 6：任务管理界面太单调，增强截止时间+提醒、子任务/清单、拖拽排序/看板、日历视图

### 验证
- `npx tsc --noEmit` 通过
- 列表视图：TaskItem 显示截止倒计时 + 子任务进度
- 看板视图：3 列拖拽改状态
- 日历视图：月历 + 优先级色点 + 当日任务

---

## 2026-07-15 Batch 6：健康维度图修复 + 心理守护概览清晰化

### 改动
- [pc/src/pages/RiskDashboardPage.tsx:393-461] 风险评分卡片强化：大色块渐变背景 + 顶部 5 格风险等级条（critical 闪烁）+ 高风险时醒目行动指引卡片（热线 400-161-9995）
- [pc/src/pages/RiskDashboardPage.tsx:579-635] 信号源分析：每信号加 4 态文字标签（良好/正常/需关注/高风险），用户立马感知问题
- [pc/src/pages/RiskDashboardPage.tsx:637-672] 风险因素列表：权重 ≥20 的严重因素用红底卡片醒目展示，其余保持默认
- [pc/src/pages/RiskDashboardPage.tsx:582,626,640] 硬编码中文（信号源分析/风险因素/权重）→ i18n `t('risk.signal_analysis')`/`t('risk.risk_factors')`/`t('risk.weight_suffix')`/`t('risk.weight_suffix_pct')`
- 健康维度雷达图修复（Batch 2 已抽 RiskRadar 组件 + Batch 5 超时部分渲染不再整页 EmptyState）

### 原因
- 需求 8：健康维度分析的图不见了（RiskRadar 组件化 + 超时部分渲染已修复）+ 心理守护概览展示要清晰，用户能立马感知问题（4 态标签 + 行动指引 + 严重因素醒目卡片）

### 验证
- `npx tsc --noEmit` 通过
- 高风险时风险卡片显示红色行动指引 + 热线
- 信号源每项显示"良好/正常/需关注/高风险"文字标签

---

## 2026-07-15 Batch 5：ONNX 准确度 + 告警逻辑优化

### 改动
- [pc/electron/services/crisisKeywords.cjs:28-33] 从 `CRISIS_EXCLUSIONS` 移除 `想死了`/`想去死`，关键词层命中即 medium，交 ONNX+Qwen 确认（避免漏报真实危机）
- [pc/electron/services/SentimentService.cjs:108-122] 关键词层负例平滑：分母 `max(total,8)` → `max(total,4)`，极短文本（<6 字）说明注释
- [pc/electron/services/SentimentService.cjs:145-166] ONNX 置信度分层：`>=0.85` 直接采信 / `0.55~0.85` 中置信 `onnx-midconf` / `<0.55` 低置信 `onnx-lowconf`
- [pc/electron/services/SentimentService.cjs:233-262] `analyzeEnhanced` 融合策略：高置信 ONNX 为主 / 中置信 ONNX 0.6+关键词 0.4 加权 / 低置信关键词 0.6+ONNX 0.4
- [pc/electron/services/SentimentService.cjs:166-184] Qwen 二次确认 prompt 强化：加 4 个 few-shot 正反例（撑不下去/笑死我了/压力大/不会想不开），降低 0.8B 模型误判
- [pc/src/components/diary/DiaryEditor.tsx:317-322] 高风险危机弹窗：30s 延迟 → 立即触发（真实危机不应延迟）
- [pc/src/pages/RiskDashboardPage.tsx:109-152] 15s 超时：不再 setError 整页 EmptyState，改为 `setTimedOut` 让页面继续渲染已有信号 + 非阻塞提示条
- [pc/src/pages/RiskDashboardPage.tsx:305-331] EmptyState 分支加 `&& !timedOut` 条件，超时时走部分渲染路径
- [pc/src/pages/RiskDashboardPage.tsx:351] 硬编码副标题 → i18n `t('risk.dashboard_subtitle')`
- [pc/src/pages/RiskDashboardPage.tsx:376-392] 超时提示条：AlertTriangle + "部分信号源加载中" + 重试按钮

### 原因
- 需求 3：提高 ONNX 判断准确度（分层置信度 + 关键词平滑 + Qwen few-shot），优化告警逻辑（DiaryEditor 立即弹窗 / RiskDashboard 部分渲染而非整页失败）

### 验证
- `npx tsc --noEmit` 通过
- DiaryEditor 保存高风险日记后立即弹危机窗
- RiskDashboardPage 超时后显示提示条 + 已有数据，非整页 EmptyState

---

## 2026-07-15 Batch 3+4：AI 助理优化 + 复盘逻辑流式化

### 改动
- [pc/src/components/assistant/ChatPanel.tsx:64-92] 回车发送延迟修复：`sendMessage` 立即调用，危机检测改为异步执行（不阻塞对话，返回 high 再弹窗）
- [pc/src/hooks/useAI.ts:17-24] 静默丢弃二次发送改为：先 abort 上一次再开始新的
- [pc/src/hooks/useAI.ts:75-90] 首 token 3s 超时计时器（仍无输出才标记重试），用 wrappedOnChunk 包装 onChunkCallback
- [pc/src/services/ai/AIService.ts:70-86] `initialize` 取消强制 `tone='counselor'`，读取 `config.tone`
- [pc/src/services/ai/AIService.ts:125,179] `buildSystemPrompt` 改用 `this.config?.tone ?? 'counselor'`
- [pc/src/services/ai/prompts.ts:62-68] 回复要求 "3-5 句" → "3-8 句，必要时可分段或用要点列表"
- [pc/src/services/ai/providers/LocalModelProvider.ts:66] 历史窗口 `slice(-4)` → `slice(-8)`（2 轮 → 4 轮）
- [pc/src/services/ai/providers/LocalModelProvider.ts:88,141] `maxTokens` 2048 → 4096
- [pc/src/components/reports/WeeklyReportCard.tsx] 全面重写：改调 `generateAIReportStream`，summary 逐字显现（打字机 + blink 光标），insights/suggestions `staggerFadeUp` 淡入；删除 "生成中..." spinner，改用 "AI 分析中" + Sparkles 脉冲
- [pc/src/services/ai/ReportAIService.ts] 新增 `generateAIReportStream(start, end, onSummaryChunk)`：流式输出 summary，JSON 解析 insights/suggestions；Qwen 失败回退 `generateWithRules`
- [pc/src/services/ai/ReportAIService.ts] system prompt 升级：加结构化输出要求 + 2 个 few-shot 示例；`maxTokens` 768→1024，`temperature` 0.5→0.6
- 删除 `pc/src/services/ai/WeeklyReportService.ts`（纯规则版，已统一为 Qwen 生成 + 规则兜底）

### 原因
- 需求 10：回车发送有延迟（危机检测 await 阻塞）、回答内容干瘪、响应速度可提升
- 需求 9：复盘统一用 Qwen，不保留纯规则主路径，升级动效看起来更智能（流式打字机 + 交错淡入，非 spinner）
- 让 AISettingsModal 的语气选择真正生效

### 验证
- `npx tsc --noEmit` 通过（仅预存 test/setup.ts vi 报错）
- 回车发送即时触发，无 2s 阻塞
- 报告生成时 summary 逐字显现 + insights/suggestions 淡入，无 "生成报告中..." spinner

---

## 2026-07-15 Batch 2：仪表盘两列重排 + 孤儿清理 + 双雷达合并

### 改动
- 删除孤儿组件 `pc/src/components/dashboard/RecentDiary.tsx`（未被 DashboardPage 渲染）
- 删除孤儿组件 `pc/src/components/dashboard/HabitSummary.tsx`（未被渲染）
- 删除孤儿组件 `pc/src/components/dashboard/MoodTrend.tsx`（未被渲染）
- 删除孤儿组件 `pc/src/components/dashboard/ProgressRing.tsx`（无引用）
- [pc/src/pages/DashboardPage.tsx:71-86] 主内容区从 `xl:grid-cols-12` 混合 col-span 重排为严格 `md:grid-cols-2` 两列，DailyQuote 跨两列置底
- [pc/src/components/emotion/RiskRadar.tsx] 新建独立 RiskRadar 组件，复用 HealthRadar 的 CSS 变量主题色（`--color-radar-stroke/fill`），替代硬编码 #14B8A6
- [pc/src/pages/RiskDashboardPage.tsx:3] import 调整：移除 Radar，新增 RiskRadar
- [pc/src/pages/RiskDashboardPage.tsx:472-484] 内联雷达图替换为 `<RiskRadar>` 组件，硬编码中文 `健康维度分析` → i18n `t('risk.health_dim')`

### 原因
- 仪表盘排版不整齐（需求 5），12 栏混合 col-span 导致 3+2+2 不规则布局
- 孤儿组件增加维护负担，DashboardPage 已不再渲染它们（需求 2.1）
- 两个雷达图概念不同但实现重复，RiskDashboardPage 内联雷达硬编码颜色/中文，合并为独立组件复用主题色 + i18n（需求 2.3）

### 验证
- `npx tsc --noEmit` 通过（仅预存 test/setup.ts vi 报错）
- 仪表盘两列整齐对齐，每卡片等高（items-stretch）
- RiskRadar 空数据时显示"暂无数据 / 记录日记/评估后将自动生成"友好提示

---

## 2026-07-15 Batch 1：Bug 修复 + Token 补全 + useClock + 删 300ms

### 改动
- [pc/src/components/layout/Sidebar.tsx:116,119] `var(--primary)` → `var(--color-primary)`（活跃指示条 + 活跃图标）
- [pc/src/index.css:682,686] `.chat-markdown a`/`blockquote` 的 `var(--primary)` → `var(--color-primary)`
- [pc/src/components/assistant/ChatMessage.tsx:16-23] `stripThinkTags` 正则修复（` IMD` → ` IMD`），并改用 `.animate-shake` 类复用全局 keyframe
- [pc/src/components/common/Textarea.tsx:17-21] 错误态统一为 `border-red-500 ring-2 ring-red-500/20`，与 Input 一致
- [pc/src/components/layout/TitleBar.tsx:82-90] 关闭按钮改 CSS `hover:bg-red-500`，移除命令式 `onMouseEnter/Leave`
- [pc/src/components/common/LockScreen.tsx:39-44,88-94] 移除局部 `@keyframes shake` 和内联 animation，复用 `.animate-shake`
- [pc/src/components/common/AnimatedNumber.tsx:29-34] 检测 `prefers-reduced-motion` 时直接跳终值
- [pc/src/index.css:97-99] 新增 `--gradient-primary` token（light + dark 两版）
- [pc/src/index.css:99] 新增 `--font-mono` token（JetBrains Mono 栈）
- [pc/src/index.css:665-681] `.chat-markdown code/pre code` 应用 `var(--font-mono)`
- [pc/tailwind.config.js:13] 新增 `fontFamily.mono`
- [pc/tailwind.config.js:36-52] 新增 `fontSize` ramp（caption/sm/base/lg/display）+ `letterSpacing`（tightest/tighter）
- [pc/src/components/layout/Sidebar.tsx:80] 硬编码渐变 → `var(--gradient-primary)`
- [pc/src/components/layout/TitleBar.tsx:55] 硬编码渐变 → `var(--gradient-primary)`
- [pc/src/components/layout/Header.tsx:114-116] 硬编码渐变 → `var(--gradient-primary)`
- [pc/src/hooks/useClock.ts] 新建：`useClock()` 每秒刷新，reduced-motion 降 60s；导出 `formatClockDisplay`/`getGreeting`
- [pc/src/components/layout/Header.tsx:39-43] 接入 `useClock`，格式 `HH:mm:ss`，删除独立 60s timer
- [pc/src/components/layout/StatusBar.tsx:8-15] 接入 `useClock`，格式 `HH:mm:ss`，删除独立 60s timer
- [pc/src/pages/DashboardPage.tsx:48-52] 问候语接入 `useClock`，跨时段自动更新
- [pc/src/pages/ReportsPage.tsx:28-42] 删除强制 300ms 最小延迟

### 原因
- 修复静默失效的 token 引用和 thinking 标签泄露，统一设计 token 与组件错误态
- 顶部时间需精确到秒、与本地系统时间实时同步（需求 4）
- 删除人为延迟以提升报表响应速度
- AnimatedNumber/LockScreen 等的无障碍与代码复用改进

### 验证
- `npx tsc --noEmit` 通过（仅预存 test/setup.ts 的 vi 报错，与本次改动无关）
- 时间显示格式 `M月D日 周X HH:mm:ss`，每秒刷新
- 仪表盘问候语随小时变化自动更新

---

## 2026-07-15 初始化索引文档

### 改动
- 新建 `E:\FriendOS\索引\README.md` — 索引总览+导航
- 新建 `E:\FriendOS\索引\01-架构总览.md`
- 新建 `E:\FriendOS\索引\02-数据库设计.md`
- 新建 `E:\FriendOS\索引\03-AI子系统.md`
- 新建 `E:\FriendOS\索引\04-情绪与风险引擎.md`
- 新建 `E:\FriendOS\索引\05-组件清单.md`
- 新建 `E:\FriendOS\索引\06-路由与页面.md`
- 新建 `E:\FriendOS\索引\07-设计系统.md`
- 新建 `E:\FriendOS\索引\08-变更日志.md`（本文件）
- 新建 `E:\FriendOS\索引\09-本次升级记录.md`

### 原因
- 建立"全局工作要求"要求的索引文档骨架，为后续每次改动同步更新提供基础。
- 通过对现有代码的全面探索，记录当前架构/DB/AI/引擎/组件/路由/设计系统的现状与已知问题。

### 验证
- 文档为只读 Markdown，无需编译。
- 代码引用均使用 `file_path:line_number` 格式，可在编辑器中点击定位。
